---
title: "Diffusion Weights"
period: "Jan 2026"
status: "in-progress"
statusDetail: "Under review · ACL '26"
topics: ["llms"]
summary: "What if you could predict where training is headed instead of running it to the end? A diffusion model forecasts a model's future weights from a few early checkpoints, then blends short, medium, and long-range guesses into the final model. About 3.2x faster, with no drop in quality."
thumbnail: "/projects/diffusion-weights.svg"
sortKey: 30
links: {}
---

Explored whether neural network fine-tuning can be reframed as a generative parameter-prediction problem instead of step-by-step SGD. A U-Net diffusion model forecasts future weight states from early checkpoints; predictions across short, medium, and long horizons are merged into the final model. On MMLU, OpenBookQA, and WMT16 the method delivered 3.2× end-to-end speedup with downstream performance comparable to full fine-tuning.
